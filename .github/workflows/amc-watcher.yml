name: AMC Showtime Watcher

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
    inputs:
      test:
        description: 'Send a test ntfy notification (uses Aug 19 which has showtimes)'
        type: boolean
        default: false

permissions:
  actions: write   # needed to disable this workflow after notifying

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
          cache: pip

      - run: pip install requests

      - name: Check showtimes
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
          WORKFLOW_FILE: amc-watcher.yml
        run: python3 check_showtimes.py ${{ inputs.test == true && '--test' || '' }}
